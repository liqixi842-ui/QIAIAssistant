# ============================================
# 动「QI」来 1.0 CRM 系统 - 生产环境配置
# ============================================

# --------------------------------------------
# 应用配置
# --------------------------------------------
NODE_ENV=production
PORT=5000

# --------------------------------------------
# 会话密钥（必填）
# --------------------------------------------
# 用于加密session的密钥（随机字符串，越长越安全）
# 生成方法：openssl rand -base64 32
SESSION_SECRET=your-random-secret-key-change-this-in-production

# --------------------------------------------
# AI 服务配置（OpenAI）
# --------------------------------------------
# OpenAI API密钥（必填）
# 从 https://platform.openai.com/api-keys 获取
AI_INTEGRATIONS_OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenAI API地址（标准 OpenAI，固定值，无需修改）
AI_INTEGRATIONS_OPENAI_BASE_URL=https://api.openai.com/v1

# AI模型（推荐使用 gpt-4o-mini，性价比高；或 gpt-4o 效果更好）
AI_MODEL=gpt-4o-mini

# --------------------------------------------
# 数据库配置（可选 - 当前版本使用内存存储）
# --------------------------------------------
# 如需使用PostgreSQL，取消下面注释并填入真实值
# DATABASE_URL=postgresql://username:password@localhost:5432/dongqilai
# PGHOST=localhost
# PGPORT=5432
# PGUSER=username
# PGPASSWORD=password
# PGDATABASE=dongqilai

# --------------------------------------------
# 会话密钥配置
# --------------------------------------------
# 用于加密session的密钥（随机字符串，越长越安全）
# 可以使用命令生成：openssl rand -base64 32
SESSION_SECRET=your-random-secret-key-here

# --------------------------------------------
# 服务器配置
# --------------------------------------------
# 服务运行端口（默认5000）
PORT=5000

# 运行环境（development 或 production）
NODE_ENV=production

# --------------------------------------------
# 可选配置
# --------------------------------------------
# AI调用超时时间（毫秒，默认30000）
AI_TIMEOUT=30000

# 缓存过期时间（小时，默认24）
CACHE_TTL_HOURS=24
